class SaveState {
	field bodies = [];
	field sprites = [];
	
	constructor(playscene) {
		// WAT? maybe just delete this? 
		/*
		bodiesToId = {};
		for (body : playscene.bodies) {
			data = [
				body.x,
				body.y,
				body.radius
			];
		}//*/
	}
}
